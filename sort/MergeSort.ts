import * as isort from "./ISort"
/**
 * MergeSort
 */
export default class MergeSort implements isort.ISort {
    
    sort( list : any[],  compareFn = (a,b) => b-a ) : any[]
    {
        return [];
    }
}