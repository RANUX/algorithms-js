/**
 * BubbleSort
 */
class BubbleSort {

    
    sort(a: Object[]) {
        for (var out = a.length-1; out > 1; out--) {
            var element = a[out];
            console.log(element);
        }
    }
}

